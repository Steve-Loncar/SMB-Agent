You are creating a gpt-image-1 poster brief for ONE OOH concept.

INPUTS

1) POSTER CONCEPT
{poster_concept}

2) SELECTED IMAGES — enriched image objects with type, intended_role, why_selected, cropping_note
(If this is a JSON object with a selected_images array and selection_notes, use selection_notes.hero_image_url as the hero.
 If this is just an array, treat the first image with intended_role "background" or "hero_focal" as the hero. Fall back to the first image in the list.)
{selected_images}

3) BRAND / DESIGN GUIDELINES (business context, vertical, target_audience, differentiators, brand colours, fonts, motifs, shortlist, OOH requirements)
{guidelines}

TASK
Identify the HERO IMAGE — the single best image for this concept. Then write a gpt-image-1 brief that produces a background plate matching the subject and style of that image.

DO NOT invent a different scene. If the hero image is beef/meat photography, the prompt is about beef/meat. If it is a person, the prompt is about a person. Stay strictly true to what the image metadata describes.

Work through these steps:
1. Identify the hero image URL (from selection_notes.hero_image_url OR first image with intended_role "background"/"hero_focal")
2. Read its metadata: type, why_selected, cropping_note, layout_pairing
3. Read the concept: image_idea, layout_notes, style_tags — use these to refine the brief
4. Confirm photographic style from guidelines.vertical and guidelines.tone
5. Write the poster_prompt:
   "Photographic UK OOH poster background plate. [subject description based on image type and why_selected]. [lighting and mood]. [composition: where subject sits, where negative space is for text overlay]. [colour palette from brand_colors]. No text, no typography, no logos, no overlaid graphics. Background plate only."

OUTPUT
Return JSON ONLY (no markdown fences, no commentary):

{
  "poster_name": "string",
  "poster_prompt": "string",
  "layout_spec": {
    "format": "6-sheet|48-sheet",
    "orientation": "portrait|landscape",
    "aspect_ratio": "1:1.5|2:1",
    "type": "classic|digital",
    "safe_area_notes": "string"
  },
  "asset_plan": {
    "hero_image_url": "exact URL from selection_notes.hero_image_url",
    "supporting_image_urls": [],
    "logo_url": "string|null",
    "use_of_brand_motifs": "string"
  },
  "render_notes": {
    "text_overlay": {
      "headline": "string (from concept)",
      "supporting_copy": "string|null",
      "cta": "string|null"
    },
    "text_safe_area": "string",
    "negative_instructions": ["string"]
  }
}

Hard checks before you answer:
- poster_prompt MUST describe the subject consistent with the hero image type and metadata — not a substitute scene, not a landscape, not an unrelated background
- hero_image_url MUST be a URL from the selected images list — do not invent or change it
- poster_prompt MUST open with "Photographic UK OOH poster background plate."
- poster_prompt MUST end with "No text, no typography, no logos, no overlaid graphics. Background plate only."
- One focal subject only — no collages, no unrelated scenes
