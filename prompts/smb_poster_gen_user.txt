You are creating a gpt-image-1 poster brief for ONE OOH concept. The poster must include BOTH a photographic scene AND typographic text rendered in the image.

INPUTS

1) POSTER CONCEPT
{poster_concept}

2) SELECTED IMAGES — enriched image objects with type, intended_role, why_selected, cropping_note
(If this is a JSON object with a selected_images array and selection_notes, use selection_notes.hero_image_url as the hero.
 If this is just an array, treat the first image with intended_role "background" or "hero_focal" as the hero. Fall back to the first image in the list.)
{selected_images}

3) BRAND / DESIGN GUIDELINES (business context, vertical, target_audience, differentiators, brand colours, fonts, motifs, shortlist, OOH requirements)
{guidelines}

TASK
Design a complete OOH poster with both photographic imagery and headline text.

If an image is attached above, LOOK AT IT — describe what you actually see (subject, lighting, textures, colours, mood) and use that as the basis for the photographic scene.

If no image is attached, use the hero image metadata to understand the subject.

Work through these steps:
1. Identify the hero image URL (from selection_notes.hero_image_url OR first image with intended_role "background"/"hero_focal")
2. If image attached: describe the real photo (subject, lighting, colour, texture, mood). If not: use metadata (type, why_selected, cropping_note)
3. Read the concept: headline, supporting_copy, cta, image_idea, layout_notes, style_tags
4. Determine text colour that contrasts with the background area where text will sit (white on dark, dark on light; use brand_colors if available)
5. Write the poster_prompt — a single paragraph that describes:
   a) "Professional UK OOH poster." (opening)
   b) The photographic background scene (subject, lighting, composition, colour palette)
   c) Where the subject sits and where negative space exists
   d) The exact headline text in quotes, with size/weight/placement (e.g. "bold white sans-serif headline reading \"Become A Butchery Pro\" across the top third")
   e) Supporting copy and CTA placement if provided
   f) "Clean professional sans-serif typography. No decorative or script fonts. No AI artefacts." (closing)

OUTPUT
Return JSON ONLY (no markdown fences, no commentary):

{
  "poster_name": "string",
  "poster_prompt": "string (complete poster brief — scene + text rendering instructions)",
  "layout_spec": {
    "format": "6-sheet|48-sheet",
    "orientation": "portrait|landscape",
    "aspect_ratio": "1:1.5|2:1",
    "type": "classic|digital",
    "text_zone": "string (where text sits)"
  },
  "asset_plan": {
    "hero_image_url": "exact URL from selection_notes.hero_image_url",
    "supporting_image_urls": [],
    "logo_url": "string|null",
    "use_of_brand_motifs": "string"
  },
  "text_content": {
    "headline": "string (verbatim from concept)",
    "supporting_copy": "string|null",
    "cta": "string|null",
    "text_colour": "string",
    "placement_description": "string"
  },
  "render_notes": {
    "negative_instructions": ["string"]
  }
}

Hard checks before you answer:
- poster_prompt MUST describe the subject consistent with the hero image — not a substitute scene
- poster_prompt MUST include the EXACT headline text in quotes with rendering instructions
- poster_prompt MUST open with "Professional UK OOH poster."
- poster_prompt MUST end with "Clean professional sans-serif typography. No decorative or script fonts. No AI artefacts."
- hero_image_url MUST be a URL from the selected images list — do not invent or change it
- text_content.headline MUST match the concept headline verbatim
- One focal subject only — no collages, no unrelated scenes
