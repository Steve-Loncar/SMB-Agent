Client URL: {url}

Business summary:
{business_summary_json}

Ad snippets from website pages (themes and proof points to match images against):
{page_snippets}
{concept_context}
Asset candidates harvested from website HTML ({asset_count} candidates after deduplication and junk filtering):
{asset_candidates_json}

Task:
1) Apply the OOH distance test and pre-filters to eliminate junk assets (icons, tiny images, social media elements, tracking pixels).
2) Score each remaining candidate using the 7-factor rubric (Snippet Theme Relevance, Vertical Fit, Brand Tone Match, Compositional Usability, Resolution/Quality, Uniqueness, Risk Deductions).{concept_task_line}
3) Select 8–20 images ranked by composite score (highest first).
4) Ensure at least 3 distinct image types are represented in your selection.
5) Identify 1–5 likely logos from the candidates using the logo detection signals.
6) Extract brand cues (colours, fonts, motifs) only where confident evidence exists in the data.
7) For each selected image, provide:
   - Which snippet themes it matches (use exact phrases from the snippets above)
   - How a poster designer should use it (background, product_focal, supporting_detail)
   - Cropping and layout guidance specific to OOH formats (portrait and landscape)
   - Any risk notes (text overlay problems, low-res indicators, potential watermarks, compliance concerns)
8) Build the shortlist: which 2–3 images work best as poster backgrounds, which as product focals, which as supporting details, and which logo to use primary.
9) If few quality candidates exist, return fewer items rather than padding with poor choices.

Output JSON only in the required visual_pack schema. Do not wrap in markdown fences.
