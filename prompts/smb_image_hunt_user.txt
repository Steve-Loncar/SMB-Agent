Client URL: {url}

Business summary:
{business_summary_json}

Ad snippets from website pages (themes and proof points to match images against):
{page_snippets}

Asset candidates harvested from website HTML ({asset_count} candidates after deduplication and junk filtering):
{asset_candidates_json}

Task:
1) Review the asset candidates above. Each has a url, kind, source_page, and optional alt/cls metadata.
2) Select the 8-20 best images for OOH poster use, ranked by quality and theme relevance.
3) Identify 1-5 likely logos from the candidates.
4) Extract brand cues (colors, fonts, motifs) only where confident.
5) For each selected image, explain:
   - Which snippet themes it matches (use exact phrases from the snippets)
   - How a poster designer should use it (background, focal product, supporting detail)
   - Cropping and layout guidance for OOH formats
   - Any risk notes (text overlay, low-res indicators, watermarks)
6) Build the shortlist: which images work best as poster backgrounds, product focals, and supporting details.
7) If few quality candidates exist, return fewer items rather than padding with poor choices.

Output JSON only in the required visual_pack schema. Do not wrap in markdown fences.