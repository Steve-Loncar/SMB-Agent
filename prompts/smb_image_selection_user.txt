You are selecting images for ONE OOH poster concept.

IMPORTANT: You may ONLY select from the candidate images provided below. Every selected URL must appear verbatim in the candidates list.

INPUTS

1) POSTER CONCEPT (single concept)
{poster_concept}

2) BRAND / DESIGN GUIDELINES (business context, brand, shortlist, OOH constraints)
{guidelines}

3) CANDIDATE IMAGES (enriched — includes type, recommended_use, cropping_guidance, layout_pairing, why_relevant, composite_score)
{visual_images}

TASK
Select the best 5–6 images for this concept. Use the enriched metadata to make better decisions:
- Prefer images where recommended_use is "background" or "product_focal"
- Prefer images that appear in guidelines.shortlist.poster_backgrounds or shortlist.product_focals
- Use cropping_guidance and layout_pairing to assess suitability for the concept's layout_notes
- Use composite_score to break ties (higher = better)
- Use type to ensure diversity (aim for background + focal + optional texture/detail)
- Reject images with risk_notes flagging text-heavy, watermarked, or very low-res issues
- Prioritise images that show the SPECIFIC products/services in guidelines.key_offers — not generic category shots
- Use guidelines.tone to filter: "premium" favours dark moody shots; "warm craft" favours golden natural light; "energetic" favours high-contrast action

For selection_notes.brand_tone_keywords: use guidelines.tone and guidelines.category as the primary source — do not infer from images alone. Combine with visual observations if they confirm the brand direction.

OUTPUT
Return JSON ONLY with this schema:

{
  "selected_images": [
    {
      "url": "exact URL from candidate list",
      "type": "hero_lifestyle|product_closeup|ingredient_texture|people_portrait|environment_location|process_craft|iconography|other",
      "intended_role": "background|hero_focal|supporting_detail|logo",
      "why_selected": "1-sentence reason referencing concept message or layout need",
      "cropping_note": "copy from candidate or summarise for this concept's format"
    }
  ],
  "selection_notes": {
    "concept_name": "string",
    "vertical": "string",
    "brand_tone_keywords": ["string"],
    "hero_image_url": "URL of the single strongest background/focal image for this concept",
    "top_reasons": ["string"],
    "rejected_reasons_summary": ["string"],
    "coverage": {
      "includes_background_candidate": true,
      "includes_focal_candidate": true,
      "includes_supporting_detail_candidate": true
    }
  }
}

Hard checks before you answer:
- Every selected_images[].url MUST appear verbatim in {visual_images}.
- hero_image_url MUST be the single best image for the concept — it will be used as the vision reference for the poster.
- Do NOT output anything except JSON.
