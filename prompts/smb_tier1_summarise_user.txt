Business website: {url}

Business context:
Name: {name_guess}
Category: {category}
Value prop: {value_prop}
Target customer: {target_customer}
Tone: {tone}

Extract advertising snippets from the page content below. Use ONLY what is written on this page.

Return JSON for this page with ad_snippets that could be used in UK roadside OOH advertising.

Rules:
- Prefer specific, factual claims (numbers, durations, locations, exclusivity)
- Avoid generic marketing fluff
- Keep snippets short

HARD CHECKS (do not skip):
- You MUST return BOTH:
  - ad_snippets: string[]
  - ad_snippets_structured: object[]
- Every ad_snippets_structured[i].source_url MUST equal page_url.
- Every ad_snippets_structured[i].quote MUST also appear verbatim in ad_snippets.
- Do NOT paraphrase into confidence="high". Use "low" if unsure.